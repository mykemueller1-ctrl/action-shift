# Cursor — what to do tonight

Clone and stay here:

```bash
git clone https://github.com/mykemueller1-ctrl/action-shift
cd action-shift
```

Do not open, clone, or PR `mykemueller1-ctrl/never86` or `never86-desk`.
Founder is asleep. Do not text him. Do not ask for POS passwords.

## Product
`docs/index.html` is the product. Netlify publishes `docs/`. jsDelivr is the morning door.
The downloadable operator/manager app stays this same loop. Read `CONSUMER.md` before adding screens.

Live pin:
https://cdn.jsdelivr.net/gh/mykemueller1-ctrl/action-shift@02af3dc/docs/index.html

## Job
1. Open `docs/index.html` in a browser.
2. Confirm defaults: House = Community Tap, manager = Kenzy, labor target = 28.
3. Confirm net and labor start blank. Do not autoload Grill dollars.
4. Paste `Net $3,408.15 Labor $1,211.85` into the PDQ box, close.
   - Labor % must be 35.56%.
   - Versus 28% that is about $258 heavy, verdict MOVE.
   - Food tile must stay HOLD unless the same-day count box is checked.
   - Send this to Kenzy copies a text. No schedule ask.
5. DoorDash box: eligible sales = Subtotal. Tips/tax/customer fees do not count.
6. PDQ Z-report PDF drop is allowed. Net = Subtotal. Labor = Labor Summary Total. Photos still do not parse.
7. If anything on that page is broken, fix only that page. Commit to this repo.

## Do not build
Eleven agents. Command Center. Void Hunter webhooks. PIN pads. Payroll. HubSpot. Vercel. Inventory. Schedule photos. Bamba dollars as CTap defaults.

## Done when
- The desk closes a night from two numbers.
- One manager text is copyable.
- Food is HOLD.
- This repo is the only repo you touched.
